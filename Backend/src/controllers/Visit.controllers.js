import fs from "fs"
export const visit = async (req, res)=> {
        const user = req.body
        const IP = req.ip
        // fs.appendFile("/temp/LOG.txt", "hello")
        // console.log("IP == ",IP,user );
        // open()

        return 
        

}